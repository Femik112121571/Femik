/*void Color(){
 digitalWrite(s2, LOW);
 digitalWrite(s3, LOW);
 red = pulseIn(out, digitalRead(out) == HIGH ? LOW : HIGH);
 digitalWrite(s3, HIGH);
 blue = pulseIn(out, digitalRead(out) == HIGH ? LOW : HIGH);
 digitalWrite(s2, HIGH);
 green = pulseIn(out, digitalRead(out) == HIGH ? LOW : HIGH); 
  if((red>blue)&&(red>green)){ko=Blue;}
  if((blue>red)&&(blue>green)){ko=Green;}
  if((green>blue)&&(green>red)){ko=Red;}
  if((red>24)&&(green>24)&&(blue>24)){ko=Wight;}
  Serial.print("Red:");
  Serial.print(red);
  Serial.print("  Blue:");
  Serial.print(blue);
  Serial.print("  Green:");
  Serial.print(green);
  Serial.print("  Color:");
  Serial.println(ko);
  delay(50);
}*/
